architecture: standalone

auth:
  enabled: true
  password: "{{{REDIS_PASSWORD}}}"

master:
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      memory: 256Mi
  persistence:
    enabled: false

replica:
  replicaCount: 0
